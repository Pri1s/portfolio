# Priyansu Biswal — Portfolio

A responsive React portfolio inspired by the supplied editorial reference.

## Run locally

```bash
npm install
npm run dev
```

Open the local URL printed by Vite.

## Build

```bash
npm run build
```

The production-ready static files are written to `dist/`.
